title: $:/rimir/llm-doc/appify/layouts
tags: $:/tags/rimir/appify/llm-doc
description: Available grid layouts, their slot names, and how to switch between them
llm-sort: 04
type: text/vnd.tiddlywiki

# Layouts

Layouts define the CSS grid structure for the app. Each layout is a tiddler tagged `$:/tags/rimir/appify/layout`.

## Available layouts

To see all layouts with their slots, render this wikitext:
```
<$list filter="[all[shadows+tiddlers]tag[$:/tags/rimir/appify/layout]]">
<$view field="caption"/>: slots=<$view field="appify-slots"/>
</$list>
```

Or use search_tiddlers with filter `[all[shadows+tiddlers]tag[$:/tags/rimir/appify/layout]]` and then get_tiddler on each.

## Built-in layouts

- **sidebar-main**: 2-column (sidebar 250px | main). Slots: `sidebar main`
- **topbar-sidebar-main**: topbar spanning full width + 2-column below. Slots: `topbar sidebar main`
- **triple-column**: 3 equal columns. Slots: `left center right`
- **dashboard**: topbar + 2 equal columns. Slots: `topbar left right`
- **focus**: single full-screen pane. Slots: `main`

## Switching layouts

Change the `appify-layout` field on the app tiddler:
```
set_field(title="<appTitle>", field="appify-layout", value="sidebar-main")
```

After switching, update `appify-view-<slot>` fields for the new layout's slots. Old slot bindings from the previous layout are preserved (in case the user switches back).

## Layout tiddler fields

- `appify-slots`: space-separated slot names
- `appify-grid-areas`: CSS grid-template-areas value
- `appify-grid-columns`: CSS grid-template-columns value
- `appify-grid-rows`: CSS grid-template-rows value
- `caption`: display name

## Deeper docs

- `$:/rimir/llm-doc/appify/proportions` — customizing column/row sizes
- `$:/rimir/llm-doc/appify/splits` — subdividing slots into panes
